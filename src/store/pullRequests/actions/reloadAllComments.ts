import { ActionContext, ActionTree } from 'vuex';
import { StoreRoot } from '../../index.d';
import { PR } from '../index.d';

export default async function reloadAllComments(
  context: ActionContext<PR, StoreRoot>,
) {
  // TODO
}
